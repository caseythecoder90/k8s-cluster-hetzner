# Q1 (topic: labels and selectors, kubectl basics)

Across **all namespaces**, find every Pod labelled `tier=demo`. Save just
their names, one per line, **sorted alphabetically**, to
`/course2/1/demo-pods` on the control-plane host.
