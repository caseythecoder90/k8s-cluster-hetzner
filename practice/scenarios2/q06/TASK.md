# Q6 (topics: rollouts, rollback)

Deployment `artemis-api` in Namespace `artemis` is not serving traffic.
Investigate why, then fix it **by rolling back** to the previous working
revision (don't just retype the correct image by hand — use the rollback
mechanism).
