apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: apps
resources:
  - ../../base

# Prod-specific tweaks live here as patches, e.g.:
# patches:
#   - patch: |-
#       - op: replace
#         path: /spec/replicas
#         value: 2
#     target:
#       kind: Deployment
#       name: personal-website

# Pin the image tag per environment:
# images:
#   - name: nginx
#     newName: ghcr.io/caseythecoder90/personal-website
#     newTag: v1.0.0
