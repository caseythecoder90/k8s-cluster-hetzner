apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: personal-website
resources:
  - ../../base

# Pin real image tags here per release instead of :latest, e.g.:
# images:
#   - name: ghcr.io/caseythecoder90/personal-website-backend
#     newTag: sha-a1b2c3d
